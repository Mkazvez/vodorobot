import React, { Component } from 'react';
import StationDataGrid from '../components/Station/';

export default class StationGrid extends Component {
    render() {
        return (
                <StationDataGrid/>
        )
    }
}
